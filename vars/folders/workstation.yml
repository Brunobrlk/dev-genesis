---
workstation_directories:
  - { path: "{{ ansible_user_dir }}/opt", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/opt/appimages", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/source", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/Development", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/.local/bin", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/.local/share/dev-genesis", mode: "0755" }
  - { path: "{{ ansible_user_dir }}/.local/share/fonts", mode: "0755" }
