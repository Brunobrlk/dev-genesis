---
- name: Skip KDE package cleanup during workstation stabilization
  ansible.builtin.debug:
    msg: "Skipping KDE package removal. KDE/PIM cleanup is intentionally not part of the stable workstation path."
# ---
# - name: Remove KDE apps replaced by preferred alternatives
#   ansible.builtin.package:
#     name:
#       - akregator
#       - kaddressbook
#       - korganizer
#       - kmail
#       - kontact
#     state: absent
#   become: true
#
# - name: Install preferred KDE replacement apps
#   ansible.builtin.package:
#     name:
#       - nemo
#       - thunderbird
#     state: present
#   become: true
#
# - name: Remove Akonadi user data directories
#   ansible.builtin.file:
#     path: "{{ item }}"
#     state: absent
#   loop:
#     - "{{ ansible_user_dir }}/.local/share/akonadi"
#     - "{{ ansible_user_dir }}/.config/akonadi"
#     - "{{ ansible_user_dir }}/.cache/akonadi"
