---
- name: Enable virtualization services
  become: true
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
    state: started
  loop: "{{ virtualization_host_services }}"
  loop_control:
    label: "{{ item }}"
