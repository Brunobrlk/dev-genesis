---
- name: Accept Android SDK licenses
  ansible.builtin.shell: |
    set -o pipefail
    yes | {{ android_sdkmanager_path | quote }} --sdk_root={{ android_sdk_root | quote }} --licenses >/dev/null
  args:
    executable: /bin/bash
  changed_when: false
